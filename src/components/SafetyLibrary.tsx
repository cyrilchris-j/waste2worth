import { Volume2 } from 'lucide-react'
import { useState } from 'react'
import type { SafetyGuide } from '../types/domain'
export function SafetyLibrary({ guides }: { guides: SafetyGuide[] }) { const [active, setActive] = useState(guides[0]); const listen = () => { if (!('speechSynthesis' in window)) return; window.speechSynthesis.cancel(); window.speechSynthesis.speak(new SpeechSynthesisUtterance(`${active.title}. ${active.instructions.join('. ')}`)) }; return <section><div className="section-heading"><div><div className="eyebrow">FIELD NOTES</div><h2>Safety, in plain language.</h2></div><button className="icon-button" title="Listen to guidance" onClick={listen}><Volume2 size={18} /> Listen</button></div><div className="safety-layout"><nav className="safety-tabs">{guides.map(guide => <button className={active.category === guide.category ? 'active' : ''} key={guide.category} onClick={() => setActive(guide)}><span>{guide.icon}</span>{guide.category}</button>)}</nav><div className="safety-content"><div className="safety-icon">{active.icon}</div><h3>{active.title}</h3><ul>{active.instructions.map(item => <li key={item}>{item}</li>)}</ul><div className="warning"><b>Safety first</b><span>{active.warnings.join(' ')}</span></div></div></div></section> }
