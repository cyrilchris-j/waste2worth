import { Check, MapPin, ShieldCheck, Star } from 'lucide-react'
import type { MatchResult } from '../types/domain'
export function MatchingList({ matches }: { matches: MatchResult[] }) { return <div className="match-list">{matches.map(({ recycler, compatibilityScore, reasons }) => <article className="match-card" key={recycler.recyclerId}><div className="match-top"><div><div className="eyebrow">FACILITY</div><h3>{recycler.facility}</h3><span className="muted"><MapPin size={14} /> {recycler.location}</span></div><div className="score"><Star size={15} fill="currentColor" /> {compatibilityScore}%</div></div><div className="match-meta"><span><ShieldCheck size={14} /> {recycler.verificationStatus === 'VERIFIED' ? 'Verified' : 'Under review'}</span><span>{recycler.capacity} kg capacity</span></div><div className="reason-list">{reasons.map(reason => <span key={reason}><Check size={13} />{reason}</span>)}</div><button className="outline-button">View facility</button></article>)}</div> }
