import { AlertTriangle, ArrowUpRight, Sparkles } from 'lucide-react'
import type { PriceEvaluation } from '../types/domain'
export function PricePanel({ evaluation }: { evaluation: PriceEvaluation }) { const status = evaluation.priceStatus.toLowerCase(); return <section className="price-panel"><div className="panel-label"><Sparkles size={15} /> Transparent price guidance</div><div className="price-grid"><div><span>Asking price</span><strong>₹{evaluation.askingPrice.toLocaleString('en-IN')}</strong></div><div><span>Reference range</span><strong>₹{evaluation.referenceRange[0].toLocaleString('en-IN')}–₹{evaluation.referenceRange[1].toLocaleString('en-IN')}</strong></div><div><span>Suggested fair value</span><strong>₹{evaluation.suggestedFairValueRange[0].toLocaleString('en-IN')}–₹{evaluation.suggestedFairValueRange[1].toLocaleString('en-IN')}</strong></div></div><div className={`price-status ${status}`}><AlertTriangle size={18} /><b>{evaluation.priceStatus}</b><span>{evaluation.deviationPercentage > 0 ? `+${evaluation.deviationPercentage}% deviation` : `${evaluation.deviationPercentage}% deviation`}</span><ArrowUpRight size={17} /></div><p>{evaluation.reason}</p></section> }
