import { describe, expect, it } from 'vitest'
import { auditLogs, demoLot } from '../data/seed'
import { LotStatus } from '../types/domain'
import { createMockPayment, settleMockPayment, transitionTransaction } from './workflow'
import type { ProcessingReport } from '../types/domain'
describe('transaction workflow', () => { const transaction = { transactionId:'TXN-1', lotId:demoLot.lotId, collectorId:demoLot.collectorId, recyclerId:'recycler-greenloop', offerId:'OFF-1', materialSummary:'Laptop lot', amount:demoLot.askingPrice, status:LotStatus.ACCEPTED, createdAt:'', updatedAt:'' }; it('creates and settles a mock payment', () => { const payment=createMockPayment(transaction); expect(payment.method).toBe('MOCK'); expect(settleMockPayment(payment,true).status).toBe('PAID') }); it('creates a report for the same lot and transaction', () => { const report: ProcessingReport = { processingReportId:'PR-1', transactionId:transaction.transactionId, lotId:transaction.lotId, recyclerId:transaction.recyclerId, receivedWeight:35, processedWeight:31, processingType:'Dismantling', recoveredMaterials:['PCB'], residualQuantity:4, processingDate:'2026-09-10', notes:'Demo report', evidence:[], createdAt:'', updatedAt:'' }; expect(report.lotId).toBe(demoLot.lotId); expect(report.transactionId).toBe(transaction.transactionId) }); it('keeps transaction IDs on transaction-linked audit events', () => { expect(auditLogs.filter(event => event.eventType === 'PRICE_EVALUATED' || event.eventType === 'MATCH_CREATED').every(event => event.transactionId === 'TXN-2026-014')).toBe(true) }); it('rejects invalid lifecycle transitions', () => { expect(() => transitionTransaction({...transaction,status:LotStatus.COMPLETED},LotStatus.LISTED)).toThrow('Invalid lifecycle transition') }) })
